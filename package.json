{
  "name": "sql-dba-linter",
  "displayName": "SQL DBA Linter",
  "description": "Validates SQL scripts against common DBA rules and best practices",
  "version": "0.0.3",
  "publisher": "nathanspencer1",
  "repository": {
    "type": "git",
    "url": "https://github.com/nathanspencer1/sql-dba-linter"
  },
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Linters"
  ],
  "activationEvents": [
    "onLanguage:sql"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sqlDbaLinter.validate",
        "title": "SQL DBA: Validate Current File"
      }
    ],
    "configuration": {
      "title": "SQL DBA Linter",
      "properties": {
        "sqlDbaLinter.requireUseStatement": {
          "type": "boolean",
          "default": true,
          "description": "Require USE {DATABASE} statement at the beginning of scripts"
        },
        "sqlDbaLinter.requireThreePartNaming": {
          "type": "boolean",
          "default": true,
          "description": "Require three-part naming convention {DATABASE}.{SCHEMA}.{TABLE}"
        },
        "sqlDbaLinter.disallowOrOperator": {
          "type": "boolean",
          "default": true,
          "description": "Disallow OR operators in queries"
        },
        "sqlDbaLinter.disallowOrderBy": {
          "type": "boolean",
          "default": true,
          "description": "Disallow ORDER BY clauses"
        },
        "sqlDbaLinter.disallowCountStar": {
          "type": "boolean",
          "default": true,
          "description": "Disallow COUNT(*) usage in queries"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "test": "node ./out/test/runTest.js",
    "lint": "eslint src --ext ts",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^18.x",
    "@types/vscode": "^1.75.0",
    "@types/mocha": "^10.0.0",
    "@types/glob": "^8.1.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@vscode/test-electron": "^2.3.0",
    "eslint": "^8.40.0",
    "glob": "^8.1.0",
    "mocha": "^10.2.0",
    "typescript": "^5.0.0",
    "@vscode/vsce": "^2.19.0"
  }
}
